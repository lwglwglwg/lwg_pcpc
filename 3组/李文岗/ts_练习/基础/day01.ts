var a:number =100
var str='hellow'

function fun(person:string){
     return 'hh'+person
}

console.log(fun('gg'));


let foo:boolean=false//字面量
let poo:Boolean=new Boolean(false)//够造函数Boolean创建的对象不是布尔值

export {} //变成局部防止报错