
//如使用第三方库 jQuery，先在 html 中通过 <script> 标签引入 jQuery，
//然后就可以使用全局变量 $ 或 jQuery 了。

// jQuery('#foo');

// 在src下新建jQuery.d.ts 里面写 declare var jQuery: (selector: string) => any;

// 或 下载第三方库  npm install @types/jquery --save-dev
  